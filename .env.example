# AI Finance Controller — Environment Variables
# Copy this file to .env and modify as needed.
# All values below have working defaults; nothing is required.

# Which batch to evaluate: "dev" or "holdout"
DATA_BATCH=dev

# Random seed for reproducible generation
SEED=42

# HTTP server port (used by docker-compose)
PORT=8000
